## 实施计划

### 1. 数据接入与处理

* 创建数据加载函数，从 `/BUG统计数据.json` 读取数据

* 提取 `metadata.fields` 作为横轴和分组的可选字段列表

* 提取 `data` 数组作为原始数据

* 添加错误处理机制

### 2. 图表实现与交互配置

* 当选择"堆积柱状图"时：

  * 横轴（X轴）默认设置为"任务类型"

  * 纵轴分组维度默认设置为"优先级"

* 实现堆积柱状图数据处理逻辑

* 添加 ECharts tooltip 交互

* 响应式布局适配

* 数据更新平滑过渡

### 3. 配置项联动

* 横轴和分组下拉框选项从数据字段动态生成

* 堆积柱状图类型时自动设置默认值

请确认后开始实施。
