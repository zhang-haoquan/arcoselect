## 修复计划

### 1. 堆积柱状图横轴排序功能优化
- 修改 `processStackBarData` 函数，添加按纵轴值总和排序
- 计算每个横轴类别的纵轴数据总和
- 根据排序配置进行升序或降序排列

### 2. 横轴排序规则生效修复
- 确保排序配置变更时正确触发数据重新计算
- 修复事件绑定和状态管理

### 3. 纵轴排序功能实现
- 实现分组字段的自定义排序（如优先级按 P0→P1→P2→P3）
- 支持正序和倒序排列
- 确保堆积柱体内部数据块顺序同步更新

请确认后开始实施修复。